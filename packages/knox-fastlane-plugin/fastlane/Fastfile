lane :test do
  knox_configure_library_upload_app(
    credential_key: ENV.fetch('CREDENTIAL_KEY', nil),
    clientIdentifierJwtToken: ENV.fetch('CLIENT_IDENTIFIER_JWT_TOKEN', nil),
    # app_name: '',
    app_description: '',
    app_file_path: '',
    api_region: 'EU'
    # package_name: ''
  )
end
